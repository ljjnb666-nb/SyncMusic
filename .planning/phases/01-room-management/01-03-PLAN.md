---
phase: 01-room-management
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/views/Room.vue
  - frontend/src/views/Home.vue
  - frontend/src/components/ParticipantList.vue
  - frontend/src/App.vue
autonomous: false
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - ROOM-05
---

<objective>
Build frontend UI: Home page with room creation, Room view with participant list and host indication, using the Pinia store and Socket.io client from Plan 02.
</objective>

<context>
@.planning/phases/01-room-management/01-01-SUMMARY.md
@.planning/phases/01-room-management/01-02-SUMMARY.md
@frontend/src/stores/room.js (from Plan 02)
@frontend/src/api/room.js (from Plan 02)
@frontend/src/socket/client.js (from Plan 02)
</context>

<interfaces>
<!-- From Plan 02 - Pinia Store Actions -->
Store Actions:
- setSessionId(id)
- setRoom(room)
- addParticipant({ userId, username })
- removeParticipant(userId)
- setHost(hostId)
- clearRoom()

Store Getters:
- isHost: boolean
- participantCount: number
- participantList: Participant[]

API Functions:
- createRoom(name, sessionId, username) -> { roomId, inviteUrl }
- getRoom(roomId) -> { id, name, hostId, createdAt } | null

Socket:
- connectToRoom(roomId, username) - connects socket and joins room
- disconnect() - disconnects socket
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Home view with room creation UI</name>
  <files>frontend/src/views/Home.vue</files>
  <read_first>
    - frontend/src/views/Home.vue (if exists)
    - frontend/src/stores/room.js (from Plan 02)
    - frontend/src/api/room.js (from Plan 02)
  </read_first>
  <action>
Create frontend/src/views/Home.vue:

1. Template:
   - Header with app name "SyncMusic"
   - Room name input field (v-model="roomName", placeholder="Enter room name")
   - "Create Room" button (disabled if roomName.trim() === '')
   - Error message display (v-if="error")
   - Loading state on button during creation

2. Script (setup):
   - Import { ref } from 'vue'
   - Import { useRouter } from 'vue-router'
   - Import { useRoomStore } from '../stores/room'
   - Import { createRoom } from '../api/room'
   - Import { getSessionId, getUsername, setUsername } from '../utils/session'
   - Import { connectToRoom } from '../socket/client'

   State:
   - roomName = ref('')
   - isCreating = ref(false)
   - error = ref('')

   Methods:
   - async handleCreateRoom():
     - Set isCreating = true, error = ''
     - Get sessionId = getSessionId()
     - Get username = getUsername()
     - Call createRoom(roomName.value.trim(), sessionId, username)
     - On success: connectToRoom(result.roomId, username), then router.push(`/room/${result.roomId}`)
     - On error: error = error.message || 'Failed to create room'
     - Set isCreating = false

3. Style:
   - Centered container, max-width 400px
   - Styled input and button with Tailwind classes
   - Error shown in red text below input
</action>
  <verify>
    <automated>grep -l "handleCreateRoom\|createRoom" frontend/src/views/Home.vue</automated>
  </verify>
  <acceptance_criteria>
    - Input validates room name is required and non-empty
    - Button disabled while creating or when name empty
    - On success: navigates to /room/:roomId
    - Error displayed to user on failure
  </acceptance_criteria>
  <done>Home view with room creation form and navigation to room</done>
</task>

<task type="auto">
  <name>Task 2: Create ParticipantList component</name>
  <files>frontend/src/components/ParticipantList.vue</files>
  <read_first>
    - frontend/src/components/ParticipantList.vue (if exists)
    - frontend/src/stores/room.js (from Plan 02)
  </read_first>
  <action>
Create frontend/src/components/ParticipantList.vue:

1. Template:
   - Container div with "Participants" heading
   - Count badge showing participantCount
   - UL list of participants:
     - Each LI shows:
       - Username (bold if isHost)
       - "(Host)" badge next to host's name
       - "You" badge if participant.id === sessionId
     - Sort by joinedAt (oldest first, hosts first within same joinedAt)
   - Empty state: "Waiting for participants..."

2. Script (setup):
   - Import { computed } from 'vue'
   - Import { useRoomStore } from '../stores/room'
   - Import { getSessionId } from '../utils/session'

   Computed:
   - sessionId = getSessionId()
   - sortedParticipants = computed(() => {
       return [...store.participants].sort((a, b) => {
         if (a.isHost && !b.isHost) return -1;
         if (!a.isHost && b.isHost) return 1;
         return a.joinedAt - b.joinedAt;
       })
     })

3. Style:
   - Card with border, rounded corners
   - Participant items with hover state
   - Host badge: gold/yellow background
   - "You" badge: blue/gray background
</action>
  <verify>
    <automated>grep -l "ParticipantList\|isHost\|sortedParticipants" frontend/src/components/ParticipantList.vue</automated>
  </verify>
  <acceptance_criteria>
    - Shows all participants sorted: hosts first, then by joinedAt
    - Host visually distinguished with "(Host)" badge and bold text
    - "You" badge shows for current user
    - Count badge shows total participant count
    - Empty state shown when no participants
  </acceptance_criteria>
  <done>ParticipantList component showing sorted participants with host and self indicators</done>
</task>

<task type="auto">
  <name>Task 3: Create Room view with join and participant display</name>
  <files>frontend/src/views/Room.vue</files>
  <read_first>
    - frontend/src/views/Room.vue (if exists)
    - frontend/src/stores/room.js (from Plan 02)
    - frontend/src/components/ParticipantList.vue (from Task 2)
    - frontend/src/socket/client.js (from Plan 02)
  </read_first>
  <action>
Create frontend/src/views/Room.vue:

1. Route: /room/:roomId

2. Template:
   - Loading state while connecting
   - Error state if room not found
   - Room header:
     - Room name (large heading)
     - Share button that copies invite URL to clipboard
     - Shows "Copied!" feedback when clicked
   - Host indicator: "You are the host" or "Hosted by [username]"
   - ParticipantList component
   - Playback area placeholder (empty div for Phase 2)
   - "No music playing" message

3. Script (setup):
   - Import { ref, onMounted, onUnmounted } from 'vue'
   - Import { useRoute, useRouter } from 'vue-router'
   - Import { useRoomStore } from '../stores/room'
   - Import { getRoom } from '../api/room'
   - Import { getSessionId, getUsername } from '../utils/session'
   - Import { connectToRoom, disconnect, socket } from '../socket/client'

   State:
   - isLoading = ref(true)
   - error = ref('')
   - copied = ref(false)
   - inviteUrl = computed(() => window.location.origin + '/room/' + route.params.roomId)

   onMounted:
   - Get roomId from route.params
   - Get sessionId and username
   - Set store.sessionId = sessionId
   - Call connectToRoom(roomId, username)
   - Set store.initSocket() to wire socket events
   - Set isLoading = false

   onUnmounted:
   - disconnect()

   Methods:
   - copyInviteLink(): navigator.clipboard.writeText(inviteUrl.value), show copied feedback

4. Watch:
   - Watch store.isConnected to show error if connection fails

5. Style:
   - Container with max-width 800px, centered
   - Room header with flex layout
   - Share button styled as secondary button
   - Placeholder for playback area (Phase 2)
</action>
  <verify>
    <automated>grep -l "Room view\|inviteUrl\|ParticipantList" frontend/src/views/Room.vue</automated>
  </verify>
  <acceptance_criteria>
    - Shows room name and share link
    - Copy button copies full invite URL to clipboard
    - ParticipantList shows all participants with host indicator
    - Shows "You are the host" if sessionId === hostId
    - Shows "Hosted by [name]" for non-hosts
    - Cleans up socket on unmount
  </acceptance_criteria>
  <done>Room view with participant display, share link, and host indicator</done>
</task>

<task type="auto">
  <name>Task 4: Setup Vue Router with Home and Room routes</name>
  <files>frontend/src/router/index.js</files>
  <read_first>
    - frontend/src/router/index.js (if exists)
    - frontend/src/App.vue (if exists)
  </read_first>
  <action>
Create/update frontend/src/router/index.js:

1. Import { createRouter, createWebHistory } from 'vue-router'
2. Import HomeView from '../views/Home.vue'
3. Import RoomView from '../views/Room.vue'
4. Define routes:
   ```javascript
   const routes = [
     { path: '/', name: 'home', component: HomeView },
     { path: '/room/:roomId', name: 'room', component: RoomView }
   ]
   ```
5. Create router = createRouter({ history: createWebHistory(), routes })
6. Export router as default

Update frontend/src/App.vue:
- Import router from './router'
- Use <router-view /> in template instead of current content
</action>
  <verify>
    <automated>grep -l "createRouter\|router-view\|path: '/room" frontend/src/router/index.js</automated>
  </verify>
  <acceptance_criteria>
    - Router configured with Home at / and Room at /room/:roomId
    - App.vue uses router-view to render matched component
  </acceptance_criteria>
  <done>Vue Router configured with Home and Room routes</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify Room UI works end-to-end</name>
  <what-built>Complete room management UI: Home create, Room view with participants, share link copy</what-built>
  <how-to-verify>
1. Start backend: cd backend && npm start
2. Start frontend: cd frontend && npm run dev
3. Open http://localhost:5173
4. Create a room named "Test Room"
5. Verify:
   - Room is created and you navigate to /room/:id
   - ParticipantList shows your username with "(Host)" badge
   - Share button copies URL to clipboard
   - "You are the host" message appears
6. Open same URL in second browser/incognito window
7. Join with different username
8. Verify:
   - Both windows show 2 participants
   - Second user does NOT have "(Host)" badge
   - First window shows new user joined
9. Close first window (host disconnects)
10. Verify:
    - Second window now shows "(Host)" badge for its user
    - "You are the host" message appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Home view allows creating a room with custom name
- [ ] Room view shows participant list in real-time
- [ ] Host is visually distinguished (bold + badge)
- [ ] Share button copies room link
- [ ] Joining from second browser shows both participants
- [ ] Host transfer occurs when original host disconnects
- [ ] Empty room cleaned up (participants === 0)
</verification>

<success_criteria>
Users can create rooms, share links, join without registration, see participants in real-time, and host transfer works correctly when host disconnects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-room-management/01-03-SUMMARY.md`
</output>

---
phase: 03-music-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/Room.vue
  - frontend/src/stores/player.js
  - frontend/src/api/music.js
requirements:
  - MUSIC-01
  - MUSIC-02
  - MUSIC-03
  - MUSIC-04
  - MUSIC-05
  - MUSIC-06
  - MUSIC-07
  - MUSIC-08
  - MUSIC-09
autonomous: false
---

<objective>
实现音乐搜索与播放的完整流程：搜索 → 下载 → 播放。当用户点击播放按钮时，触发后端下载歌曲（yt-dlp），然后通过 AudioPlayer 播放本地文件。
</objective>

<context>
当前状态：
- 后端 /api/music/search 已实现多平台搜索（酷狗、网易云、咪咕）
- 后端 /api/music/download 已实现 yt-dlp 下载
- 前端 MusicParser 可以搜索并显示结果
- 前端 Room.vue 有播放列表和 AudioPlayer
- 已添加本地音乐导入功能（URL.createObjectURL）
- Phase 2 的 socket 同步播放已实现
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现搜索结果直接播放</name>
  <files>frontend/src/views/Room.vue</files>
  <read_first>
    - frontend/src/views/Room.vue
    - frontend/src/stores/player.js
    - frontend/src/api/music.js
  </read_first>
  <action>
修改 onSongAdded 函数，支持搜索结果直接播放：

当 isHost 点击搜索结果的播放按钮时：
1. 调用 browserDownload API 下载歌曲到服务器
2. 下载完成后，获得文件路径
3. 将歌曲添加到播放列表
4. 如果是当前选中的歌曲，触发播放

修改 Room.vue 中的播放逻辑：
- 当 roomStore.isHost && playerStore.currentSong 时，点击播放按钮
- 调用 audioPlayerRef.value.play()

确保 playerStore.currentSong 包含正确的音频 URL（blobUrl 或 /downloads/xxx）。
  </action>
  <verify>
    <automated>grep -l "browserDownload\|play()" frontend/src/views/Room.vue</automated>
  </verify>
  <acceptance_criteria>
    - 点击搜索结果的播放按钮，歌曲开始下载
    - 下载完成后，歌曲自动播放
    - 非房主不能触发下载/播放
  </acceptance_criteria>
  <done>搜索结果可以直接播放</done>
</task>

<task type="auto">
  <name>Task 2: 添加播放状态指示器</name>
  <files>frontend/src/views/Room.vue</files>
  <read_first>
    - frontend/src/views/Room.vue
  </read_first>
  <action>
在 Room.vue 中添加下载/播放状态：
- 添加 isDownloading ref
- 添加 downloadProgress ref（0-100）
- 显示下载进度条
- 下载完成后隐藏进度条

在 MusicParser 的搜索结果中，点击播放按钮时：
- 按钮显示 loading 状态
- 禁用其他操作
- 下载完成后恢复
  </action>
  <verify>
    <automated>grep -l "isDownloading\|downloadProgress" frontend/src/views/Room.vue</automated>
  </verify>
  <acceptance_criteria>
    - 下载时显示进度指示
    - 播放状态正确显示
  </acceptance_criteria>
  <done>播放状态指示器已添加</done>
</task>

<task type="auto">
  <name>Task 3: 验证完整播放流程</name>
  <files>frontend/src/views/Room.vue</files>
  <read_first>
    - frontend/src/views/Room.vue
    - backend/src/routes/music.js
  </read_first>
  <action>
测试完整流程：
1. 房主搜索歌曲（周杰伦）
2. 点击搜索结果的播放按钮
3. 观察后端日志：yt-dlp 下载过程
4. 观察前端：下载进度显示
5. 歌曲开始播放

如果 yt-dlp 不可用或下载失败：
- 显示友好的错误提示
- 记录错误信息到控制台
  </action>
  <verify>
    <automated>grep -l "download\|playback" backend/src/routes/music.js</automated>
  </verify>
  <acceptance_criteria>
    - 完整播放流程：搜索 → 下载 → 播放
    - 错误处理：下载失败时显示错误信息
  </acceptance_criteria>
  <done>完整播放流程验证通过</done>
</task>

</tasks>

<verification>
- [ ] 搜索歌曲得到结果
- [ ] 点击播放按钮触发下载
- [ ] 下载完成后歌曲自动播放
- [ ] 听众端同步看到当前播放歌曲
</verification>

<success_criteria>
房主可以搜索歌曲，点击播放后自动下载并播放，听众可以看到当前播放的歌曲并同步播放。
</success_criteria>
